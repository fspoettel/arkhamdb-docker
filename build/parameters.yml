parameters:
    database_host:     '%env(MYSQL_DOCKER_HOST_NAME)%'
    database_name:     '%env(MYSQL_DATABASE)%'
    database_user:     '%env(MYSQL_USER)%'
    database_password: '%env(MYSQL_PASSWORD)%'

    mailer_transport:  smtp
    mailer_host:       127.0.0.1
    mailer_user:       smtp_user
    mailer_password:   smtp_password

    # A secret key that's used to generate certain security-related tokens
    secret:            ThisTokenIsNotSoSecretChangeIt
    
    # units: seconds
    cache_expiration:  600
    
    # Email sender
    email_sender_address: mail@example.com
    email_sender_name: Example Mailer
    
    # Branding
    website_name: ArkhamDb
    website_url: localhost
    game_name: Arkham Horror LCG
    publisher_name: Fantasy Flight Games
    
    # Analytics & AdSense
    google_analytics_tracking_code: UA-00000000-1
    google_adsense_client: ca-pub-000000000000000
    google_adsense_slot: 0000000000
